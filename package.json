{
  "name": "video-speed-controller-extension",
  "version": "1.4.0",
  "private": true,
  "scripts": {
    "check": "npm run check:syntax && npm test",
    "check:syntax": "node --check background/service-worker.js && node --check content/content.js && node --check popup/popup.js && node --check shared/settings.js && node scripts/validate-manifest.js",
    "package": "npm run check && node scripts/package-extension.js",
    "test:manual": "node scripts/serve-test-page.js",
    "test": "node --test"
  }
}
